# ServiceStore v17 Starry
1. Run `supabase/servicestore-v17-starry-safe.sql` once in Supabase SQL Editor.
2. Replace repository files and push to GitHub.
3. Open `login.html` for sign in and `admin.html?v=17` for admin.

The migration does not delete users, credits, orders, slips, or history.
