# ServiceStore V22.2 Service Button Hotfix

- Fixed service buttons not opening the hire page.
- Replaced calls to the missing `showPage()` function with the existing router.
- Fixed dynamic service loading failing because a constant result variable was reassigned.
- Prevented decorative card layers from blocking button clicks.
- Removed duplicated nested project files, embedded `.git`, and obsolete version files.
